/**
 * Load TanStack Start's public type entrypoint so its module augmentations
 * for TanStack Router (including server-route options) are part of the
 * TypeScript program.
 */
import "@tanstack/react-start";
